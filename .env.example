# OcularMP4 does not require server-owned AI credentials.
# Users configure their own provider, model, endpoint, and API key in Settings.

# Optional public URL used by deployment metadata.
APP_URL="http://localhost:3000"
